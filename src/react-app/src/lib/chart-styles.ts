export const TOOLTIP_STYLE = {
  borderRadius: 8,
  border: "1px solid hsl(var(--border))",
  backgroundColor: "hsl(var(--card))",
  color: "hsl(var(--card-foreground))",
};

export const TICK_STYLE = { fill: "hsl(var(--muted-foreground))", fontSize: 12 };
